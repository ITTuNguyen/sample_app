<li>
  <%= gravatar_for user, size: Settings.user.size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t("layout.user.del"), user, method: :delete,
      data: {confirm: t("layout.user.conf")} %>
  <% end %>
</li>
